## Authoring Workflows / multi-agent runs

When authoring a `Workflow` or multi-agent run that produces facts or numbers, follow the reliability rules in `~/.claude/workflows/RELIABILITY.md`, and start fact/research-heavy runs from `~/.claude/workflows/reliable-research.js`.
